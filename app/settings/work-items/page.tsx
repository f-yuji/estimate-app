import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function WorkItems(){const items=await prisma.workItemMaster.findMany({include:{category:true},orderBy:{displayOrder:"asc"}});return <><div className="topline"><div><h1>工事項目設定</h1><div className="hint">名称ではなく項目コードで数量連動します</div></div><button className="button">＋ 項目追加</button></div><div className="table-wrap"><table><thead><tr><th>コード</th><th>表示名</th><th>工種</th><th>単位</th><th>標準売単価</th><th>標準原価</th><th>数量参照元</th><th>状態</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.itemCode}</td><td>{x.displayName}</td><td>{x.category.name}</td><td>{x.unit}</td><td className="money">{Number(x.standardSalesPrice).toLocaleString()}</td><td className="money">{Number(x.standardCost).toLocaleString()}</td><td>{x.defaultQuantitySource}</td><td><span className="badge">{x.active?"有効":"無効"}</span></td></tr>)}</tbody></table></div></>}
