import { prisma } from "@/lib/prisma";import { saveCompany } from "@/app/actions";
export const dynamic="force-dynamic";
export default async function Company(){const c=await prisma.companySetting.findUnique({where:{id:1}});return <><div className="topline"><h1>会社情報設定</h1></div><form action={saveCompany} className="card"><div className="grid"><div className="field"><label>会社名</label><input name="companyName" defaultValue={c?.companyName}/></div><div className="field"><label>電話番号（044系を正として登録）</label><input name="phone" pattern="044[-0-9() ]+" defaultValue={c?.phone??""}/></div><div className="field" style={{gridColumn:"1/-1"}}><label>住所</label><input name="address" defaultValue={c?.address??""}/></div></div><button className="button" style={{marginTop:20}}>保存</button></form></>}
